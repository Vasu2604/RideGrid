# Example environment variables (do NOT store secrets here)
# Copy this to .env and fill in secrets locally before running.

# Database
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=ridegrid
POSTGRES_PASSWORD=ridegrid_dev
POSTGRES_DB=ridegrid

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379

# Kafka/Redpanda
KAFKA_BROKER=localhost:9092

# JWT secret (use a secure value in production)
JWT_SECRET=dev-secret-change-me

# Other examples
# MONGO_URL=mongodb://user:password@localhost:27017
# SENTRY_DSN=
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
